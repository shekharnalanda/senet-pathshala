@extends('admin.layouts.app')
@section('title','Results')
@section('heading','Results')
@section('content')
<div class="mb-4"><h1 class="fw-bold mb-1">Results Management</h1><p class="text-secondary mb-0">Enter marks, edit results and print/download report cards from Admin.</p></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<div class="card border-0 shadow-sm mb-4"><div class="card-body p-4"><h5 class="fw-bold mb-3"><i class="fa-solid fa-print text-success me-2"></i>Bulk Report Cards</h5><form method="GET" action="{{ route('admin.results.bulk-report-cards') }}" target="_blank"><div class="row g-3"><div class="col-md-5"><label class="form-label">Exam *</label><select name="exam" class="form-select" required><option value="">Select Exam</option>@foreach($examNames as $examName)<option value="{{ $examName }}">{{ $examName }}</option>@endforeach</select></div><div class="col-md-5"><label class="form-label">Class</label><select name="class_name" class="form-select"><option value="">All Classes</option>@foreach($classNames as $className)<option value="{{ $className }}">{{ $className }}</option>@endforeach</select></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-success w-100">Bulk Print</button></div></div></form><div class="form-text mt-2">One report card per A4 page. In print dialog, choose “Save as PDF” to download all selected report cards as one PDF.</div></div></div>
<div class="card border-0 shadow-sm mb-4"><div class="card-body p-4"><h5 class="fw-bold mb-3">Add / Update Marks</h5><form method="POST" action="{{ route('admin.results.store') }}">@csrf<div class="row g-3"><div class="col-lg-4"><label class="form-label">Student *</label><select name="student_id" class="form-select" required><option value="">Select Student</option>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->admission_no }} — {{ $student->user->name }} ({{ $student->class_name }})</option>@endforeach</select></div><div class="col-lg-3"><label class="form-label">Exam *</label><input name="exam_name" class="form-control" required placeholder="First Term"></div><div class="col-lg-3"><label class="form-label">Subject *</label><input name="subject" class="form-control" required placeholder="Mathematics"></div><div class="col-lg-2"><label class="form-label">Exam Date</label><input type="date" name="exam_date" class="form-control"></div><div class="col-md-2"><label class="form-label">Max Marks *</label><input type="number" step="0.01" min="0.01" name="max_marks" value="100" class="form-control" required></div><div class="col-md-2"><label class="form-label">Marks Obtained *</label><input type="number" step="0.01" min="0" name="marks_obtained" class="form-control" required></div><div class="col-md-6"><label class="form-label">Remark</label><input name="remark" class="form-control" placeholder="Good / Needs improvement"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Save Result</button></div></div></form></div></div>
<div class="card border-0 shadow-sm"><div class="card-body p-4"><h5 class="fw-bold mb-3">Result Entries</h5><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Student</th><th>Exam</th><th>Subject</th><th>Marks</th><th>%</th><th>Remark</th><th>Actions</th></tr></thead><tbody>@forelse($results as $result)@php($max=(float)$result->max_marks)@php($obt=(float)$result->marks_obtained)@php($pct=$max>0?round(($obt/$max)*100,1):0)<tr><td><strong>{{ $result->student->user->name }}</strong><br><small class="text-secondary">{{ $result->student->admission_no }}</small></td><td>{{ $result->exam_name }}</td><td>{{ $result->subject }}</td><td>{{ number_format($obt,2) }} / {{ number_format($max,2) }}</td><td><strong>{{ $pct }}%</strong></td><td>{{ $result->remark ?: '—' }}</td><td><div class="d-flex flex-wrap gap-1"><a href="{{ route('admin.results.report-card',['student'=>$result->student_id,'exam'=>$result->exam_name]) }}" target="_blank" class="btn btn-sm btn-success" title="Print or download complete report card"><i class="fa-solid fa-file-lines"></i> Report Card</a><a href="{{ route('admin.results.edit',$result) }}" class="btn btn-sm btn-outline-primary"><i class="fa-solid fa-pen"></i> Edit</a><form method="POST" action="{{ route('admin.results.destroy',$result) }}" onsubmit="return confirm('Delete this result?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="fa-solid fa-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="7" class="text-center text-secondary py-4">No results entered yet.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection