@extends('admin.layouts.app')
@section('title','Online Admissions')
@section('heading','Online Admissions')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="fw-bold mb-1">Online Admission Applications</h1><p class="text-secondary mb-0">Review applications submitted from the website before final approval.</p></div><form method="GET" class="d-flex gap-2"><select name="status" class="form-select"><option value="">All Status</option>@foreach(['pending'=>'Pending','approved'=>'Approved','rejected'=>'Rejected'] as $v=>$label)<option value="{{ $v }}" {{ request('status')===$v?'selected':'' }}>{{ $label }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Application</th><th>Student</th><th>Class</th><th>Parent / Phone</th><th>Date</th><th>Status</th><th></th></tr></thead><tbody>@forelse($applications as $application)<tr><td class="fw-semibold">{{ $application->application_no }}</td><td>{{ $application->student_name }}</td><td>{{ $application->class_applied }}</td><td>{{ $application->father_name ?: ($application->guardian_name ?: '—') }}<br><small class="text-secondary">{{ $application->phone }}</small></td><td>{{ $application->created_at->format('d M Y') }}</td><td><span class="badge {{ $application->status==='approved'?'bg-success':($application->status==='rejected'?'bg-danger':'bg-warning text-dark') }}">{{ ucfirst($application->status) }}</span></td><td><a href="{{ route('admin.admission-applications.show',$application) }}" class="btn btn-sm btn-outline-primary">View Details</a></td></tr>@empty<tr><td colspan="7" class="text-center text-secondary py-5">No online admission applications yet.</td></tr>@endforelse</tbody></table></div>@if($applications->hasPages())<div class="card-footer bg-white">{{ $applications->links() }}</div>@endif</div>
@endsection
