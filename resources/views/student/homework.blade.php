@extends('layouts.app')
@section('title','My Homework')
@section('content')
<section class="py-5 bg-light min-vh-100"><div class="container"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h3 fw-bold mb-1">My Homework</h1><p class="text-secondary mb-0">{{ $student->class_name ?: 'Class not assigned' }}{{ $student->section ? ' / '.$student->section : '' }}</p></div><a href="{{ route('student.dashboard') }}" class="btn btn-outline-primary">← Dashboard</a></div>
<div class="row g-3">@forelse($homeworks as $homework)<div class="col-12"><div class="card border-0 shadow-sm"><div class="card-body p-4"><div class="d-flex flex-wrap justify-content-between gap-2 mb-2"><div><span class="badge bg-primary mb-2">{{ $homework->subject }}</span><h5 class="fw-bold mb-1">Homework for {{ $homework->homework_date->format('d M Y') }}</h5></div>@if($homework->due_date)<div class="text-end"><small class="text-secondary d-block">Due Date</small><strong class="{{ $homework->due_date->isPast() ? 'text-danger' : 'text-success' }}">{{ $homework->due_date->format('d M Y') }}</strong></div>@endif</div><p class="mb-0" style="white-space:pre-line">{{ $homework->details }}</p></div></div></div>@empty<div class="col-12"><div class="card border-0 shadow-sm"><div class="card-body text-center text-secondary py-5">No homework has been published for your class yet.</div></div></div>@endforelse</div></div></section>
@endsection
