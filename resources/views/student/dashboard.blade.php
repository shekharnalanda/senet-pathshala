@extends('layouts.app')
@section('title','Student Dashboard')
@section('content')
<section class="py-5 bg-light"><div class="container"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h3 fw-bold mb-1">Welcome, {{ auth()->user()->name }}</h1><p class="text-secondary mb-0">Student Dashboard · C-Net Pathshala</p></div><form method="POST" action="{{ route('student.logout') }}">@csrf<button class="btn btn-outline-danger">Logout</button></form></div><div class="row g-4 mb-4">@foreach([['Admission No.',$student->admission_no],['Class / Section',($student->class_name?:'—').($student->section?' / '.$student->section:'')],['Roll No.',$student->roll_no?:'—']] as [$l,$v])<div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary small">{{ $l }}</div><div class="fs-5 fw-semibold">{{ $v }}</div></div></div></div>@endforeach</div><div class="row g-3">
@foreach([[route('student.profile'),'Profile','Personal and guardian details'],[route('student.id-card'),'Digital ID Card','Student identity card with printable details'],[route('student.attendance'),'Attendance','Daily attendance and overall percentage'],[route('student.homework'),'Homework','Assignments and homework for your class'],[route('student.results'),'Results','Exam marks and report cards'],[route('student.fees'),'Fee Status','Paid and pending fees with receipts']] as [$url,$title,$text])<div class="col-md-6 col-lg-4"><a href="{{ $url }}" class="text-decoration-none text-reset"><div class="card border-0 shadow-sm h-100"><div class="card-body"><h5 class="fw-bold">{{ $title }}</h5><p class="text-secondary mb-0">{{ $text }}</p></div></div></a></div>@endforeach
<div class="col-md-6 col-lg-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><h5 class="fw-bold">Certificates</h5><p class="text-secondary mb-0">Available certificates and documents</p></div></div></div></div></div></div></section>
@endsection