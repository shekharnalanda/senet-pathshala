@extends('layouts.app')
@section('title','My Attendance')
@section('content')
<section class="py-5 bg-light min-vh-100"><div class="container"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h3 fw-bold mb-1">My Attendance</h1><p class="text-secondary mb-0">{{ auth()->user()->name }} · {{ $student->admission_no }}</p></div><a href="{{ route('student.dashboard') }}" class="btn btn-outline-primary">← Dashboard</a></div>
<div class="row g-3 mb-4"><div class="col-6 col-lg-3"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary small">Attendance</div><div class="display-6 fw-bold text-primary">{{ number_format($percentage,1) }}%</div></div></div></div><div class="col-6 col-lg-3"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary small">Present</div><div class="display-6 fw-bold text-success">{{ $present }}</div></div></div></div><div class="col-6 col-lg-3"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary small">Absent</div><div class="display-6 fw-bold text-danger">{{ $absent }}</div></div></div></div><div class="col-6 col-lg-3"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary small">Leave</div><div class="display-6 fw-bold text-warning">{{ $leave }}</div></div></div></div></div>
<div class="card border-0 shadow-sm"><div class="card-body p-4"><h5 class="fw-bold mb-3">Attendance History</h5><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Day</th><th>Status</th><th>Remark</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->attendance_date->format('d M Y') }}</td><td>{{ $record->attendance_date->format('l') }}</td><td><span class="badge {{ $record->status==='Present' ? 'bg-success' : ($record->status==='Absent' ? 'bg-danger' : 'bg-warning text-dark') }}">{{ $record->status }}</span></td><td>{{ $record->remark ?: '—' }}</td></tr>@empty<tr><td colspan="4" class="text-center text-secondary py-4">Attendance has not been recorded yet.</td></tr>@endforelse</tbody></table></div></div></div></div></section>
@endsection
