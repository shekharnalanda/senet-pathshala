@extends('layouts.app')
@section('title','My Fee Status')
@section('content')
<section class="py-5 bg-light min-vh-100"><div class="container"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h3 fw-bold mb-1">My Fee Status</h1><p class="text-secondary mb-0">{{ auth()->user()->name }} · {{ $student->admission_no }}</p></div><a href="{{ route('student.dashboard') }}" class="btn btn-outline-primary">← Dashboard</a></div>
<div class="row g-3 mb-4"><div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary">Total Fee</div><div class="fs-3 fw-bold">₹{{ number_format($totalFee,2) }}</div></div></div></div><div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary">Total Paid</div><div class="fs-3 fw-bold text-success">₹{{ number_format($totalPaid,2) }}</div></div></div></div><div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="text-secondary">Pending Fee</div><div class="fs-3 fw-bold {{ $pending>0?'text-danger':'text-success' }}">₹{{ number_format($pending,2) }}</div></div></div></div></div>
<div class="card border-0 shadow-sm"><div class="card-body p-4"><h5 class="fw-bold mb-3">Payment History</h5><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Receipt No.</th><th>Amount Paid</th><th>Mode</th><th>Note</th><th></th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->payment_date?->format('d M Y') }}</td><td><strong>{{ $payment->receipt_no }}</strong></td><td class="text-success fw-bold">₹{{ number_format((float)$payment->amount_paid,2) }}</td><td>{{ $payment->payment_mode }}</td><td>{{ $payment->note ?: '—' }}</td><td class="text-end"><a target="_blank" href="{{ route('student.fee-receipt',$payment) }}" class="btn btn-sm btn-outline-primary">View Receipt</a></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-5">No fee payment has been recorded yet.</td></tr>@endforelse</tbody></table></div></div></div></div></section>
@endsection